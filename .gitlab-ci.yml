image: node:latest

stages:
 - build
 - test
  
install_dependencies:
 stage: build
 script:
  - npm install
  - chmod -R u+x ./.bin
 artifacts:
  paths:
   - tig_client/node_modules/
   - node_modules/
   
testing:
 stage: test
 services:
  - mongo:latest

 variables:
  MONGO_DATABASE: test

  Host: cluster0-e8bno.gcp.mongodb.net
  Database: test
  
 script:
  - npm run test
 artifacts:
  paths:
   - tig_client/